server:
  port: 5050

spring:
  application:
    name: citypulse-backend-springboot
  datasource:
    url: jdbc:h2:mem:citypulsedb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false

google:
  ai:
    studio:
      api-key: ${GEMINI_API_KEY:AIzaSyDemoCityPulseApiKey2026}
